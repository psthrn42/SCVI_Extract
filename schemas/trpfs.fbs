file_extension "trpfs";

// The first 8 bytes contain the magic value 'ONEPACK'
// The next 8 bytes contain the offset to the root table (0x0141E72FC0 in leaked scarlet version)

table TRPFS {
	hashes: [uint64]; // Offset 0x141E92764
	file_offsets: [uint64];
}

root_type TRPFS;